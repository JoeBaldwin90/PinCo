<h2><%= @product.title %></h2>
<h3>£<%= @product.price %></h3>

<div>
  <%= image_tag @product.image_1.thumb.to_s %>
</div>

<p><%= simple_format @product.description %></p>

<%= simple_form_for [@product, @order_item] do |f| %>
  <%= f.input :quantity %>
  <%= f.button :submit, "Add to cart" %>
<% end %>

<p><%= link_to "Back to all products", products_path %></p>
